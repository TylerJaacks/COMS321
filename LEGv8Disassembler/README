1. Run the build.sh script to build the program.
2. Modify the run.sh to change the file you wish to disassemble.
3. Run the run.sh

Example:

$ ./build.sh
$ vim run.sh # Change the filename
$ ./run.sh